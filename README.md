rpi-scripts
===========

Shell scripts and notes for raspberry-pi

#### Vi arrow keys

```
~/.vimrc

set nocp
set nocompatible
```

#### Aliases
```
~/.bashrc

alias ls='ls --color=auto'
alias ll='ls -l'
alias la='ls -al'
alias vi='vim'
```
